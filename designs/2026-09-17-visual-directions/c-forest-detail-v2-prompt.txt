Use case: precise-object-edit. Edit the attached existing romi UI prototype image. Change ONLY the command text inside the "运行命令" input field in the lower-right "节点凭证" drawer: replace the existing text "romi connect SG-Edge-01" with the exact literal text "./monitor-agent". It must read exactly ./monitor-agent with initial period, forward slash, and hyphen. Keep its monospace styling, position, field border and copy icon. Preserve EVERY other pixel-level visual feature as closely as possible: all other text, masked credential dots, button labels, both full screens, layout, spacing, typography, colors, chart, tables, state strip, and image dimensions. Do not redesign, add anything, crop, or change any other field.
